# 使用

使用之前，请先[安装](#/doc/install)。

引入YU UI

```
import {Button, Tabs, Switch, Dialog} from "king-ui"
```

代码示例：

```
<template>
  <div><Button>按钮</Button></div>
</template>
<script>
  import {Button, Tabs, Switch, Dialog} from "king-ui"
  export default {
    components: {Button}
  }
</script>
```